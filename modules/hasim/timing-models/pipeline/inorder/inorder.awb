%AWB_START

%name Inorder Pipeline
%desc Inorder Pipeline
%provides hasim_pipeline
%requires inorder_pipeline_front_end inorder_pipeline_back_end pipeline_base_types 

%attributes hasim model pipeline inorder

%public inorder-pipeline.bsv
%public inorder-pipeline.h

%param SYNTH_BOUNDARY mkPipeline "Name of synthesis boundary"

%AWB_END
