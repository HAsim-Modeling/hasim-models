%name Split (Instruction and Data)
%desc Split L1 caches (Instruction and Data)

%provides hasim_l1_caches
%requires hasim_itlb
%requires hasim_l1_icache
%requires hasim_dtlb
%requires hasim_l1_dcache
%requires hasim_miss_tracker
%requires hasim_l1_arbiter
%requires l1_cache_base_types

%attributes hasim model caches

%public l1-caches.bsv

%param SYNTH_BOUNDARY mkL1Cache "Name of synthesis boundary"
