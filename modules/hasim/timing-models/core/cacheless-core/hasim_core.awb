%name Cacheless Core
%desc Cacheless Core

%provides hasim_core

%requires hasim_pipeline

%attributes hasim cacheless core

%public Core.bsv

%param SYNTH_BOUNDARY mkCore "Name of synthesis boundary"
